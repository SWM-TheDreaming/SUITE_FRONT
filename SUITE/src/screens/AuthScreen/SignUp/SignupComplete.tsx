import React from 'react';
import { View, Text } from 'react-native';

const SignupComplete = () => {
  return (
    <View>
      <Text>SignupComplete page</Text>
    </View>
  );
};

export default SignupComplete;
